{
  "name": "@conversation-platform/gateway-service",
  "version": "0.1.0",
  "private": true,
  "description": "API Gateway service for routing and rate limiting",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/config": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
