{
  "name": "@conversation-platform/context-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Context management engine for conversations, tenants, users, sessions, and messages",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "engines": {
    "node": ">=20"
  }
}
