{
  "name": "@conversation-platform/campaign-executor",
  "version": "0.1.0",
  "private": true,
  "description": "Campaign execution engine with queue, scheduler, rate limiter, and worker management",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/database": "workspace:*",
    "@conversation-platform/queue": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/campaign": "workspace:*",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=20"
  }
}
