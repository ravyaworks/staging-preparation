{
  "name": "@conversation-platform/model-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Model registry and provider capability management",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/provider-framework": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
