{
  "name": "@conversation-platform/vector-search",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-independent vector search and retrieval layer with ranking, filtering, scoring, and hybrid search",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/config": "workspace:*",
    "@conversation-platform/shared": "workspace:*"
  },
  "engines": { "node": ">=20" }
}
