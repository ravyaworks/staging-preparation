{
  "name": "@conversation-platform/delivery-tracking",
  "version": "0.1.0",
  "private": true,
  "description": "Delivery tracking and job monitoring system",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/database": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/campaign-executor": "workspace:*",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=20"
  }
}
