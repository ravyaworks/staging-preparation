{
  "name": "@conversation-platform/workflow-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Visual-workflow-ready engine supporting definitions, execution, conditions, rules, actions, triggers, and scheduling",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/config": "workspace:*",
    "@conversation-platform/shared": "workspace:*",
    "@conversation-platform/event-bus": "workspace:*",
    "@conversation-platform/queue": "workspace:*"
  },
  "engines": { "node": ">=20" }
}
