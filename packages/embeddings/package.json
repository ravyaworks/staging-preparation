{
  "name": "@conversation-platform/embeddings",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-independent embedding generation, storage, caching, and batch processing abstraction layer",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/config": "workspace:*",
    "@conversation-platform/provider-framework": "workspace:*"
  },
  "engines": { "node": ">=20" }
}
