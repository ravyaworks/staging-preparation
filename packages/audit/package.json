{
  "name": "@conversation-platform/audit",
  "version": "0.1.0",
  "private": true,
  "description": "Audit system tracking knowledge, workflow, tool, plugin, user, system, and configuration changes",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/config": "workspace:*",
    "@conversation-platform/shared": "workspace:*",
    "@conversation-platform/database": "workspace:*"
  },
  "engines": { "node": ">=20" }
}
