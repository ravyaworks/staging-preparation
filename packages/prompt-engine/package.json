{
  "name": "@conversation-platform/prompt-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Prompt management, templates, versioning, and composition engine",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "engines": {
    "node": ">=20"
  }
}
