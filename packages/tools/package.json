{
  "name": "@conversation-platform/tools",
  "version": "0.1.0",
  "private": true,
  "description": "Default tool abstractions for Calendar, CRM, Email, HTTP, Database, Storage, Search, Webhook, Notification, and more",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/config": "workspace:*",
    "@conversation-platform/shared": "workspace:*",
    "@conversation-platform/tool-engine": "workspace:*",
    "@conversation-platform/storage": "workspace:*"
  },
  "engines": { "node": ">=20" }
}
