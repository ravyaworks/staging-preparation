{
  "name": "@conversation-platform/memory-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Conversation memory management with short-term, long-term, and persistent storage interfaces",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "engines": { "node": ">=20" }
}
