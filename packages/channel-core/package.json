{
  "name": "@conversation-platform/channel-core",
  "version": "0.1.0",
  "private": true,
  "description": "Core channel abstractions, message model, registry, and manager for omnichannel communication",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/shared": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/event-bus": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
