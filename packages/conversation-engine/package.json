{
  "name": "@conversation-platform/conversation-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Conversation lifecycle, state management, message processing, and response pipeline engine",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/ai-engine": "workspace:*",
    "@conversation-platform/prompt-engine": "workspace:*",
    "@conversation-platform/memory-engine": "workspace:*",
    "@conversation-platform/context-engine": "workspace:*",
    "@conversation-platform/logger": "workspace:*"
  },
  "engines": { "node": ">=20" }
}
