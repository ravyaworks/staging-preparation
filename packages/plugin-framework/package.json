{
  "name": "@conversation-platform/plugin-framework",
  "version": "0.1.0",
  "private": true,
  "description": "Modular plugin architecture with registration, loading, lifecycle, permissions, and event system",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/logger": "workspace:*",
    "@conversation-platform/config": "workspace:*",
    "@conversation-platform/shared": "workspace:*",
    "@conversation-platform/event-bus": "workspace:*"
  },
  "engines": { "node": ">=20" }
}
