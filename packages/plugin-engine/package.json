{
  "name": "@conversation-platform/plugin-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Plugin execution engine for third-party extensions with sandboxed execution, tool registration, workflow hooks, and event system",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/plugin-framework": "workspace:*",
    "@conversation-platform/event-bus": "workspace:*",
    "@conversation-platform/logger": "workspace:*"
  },
  "engines": { "node": ">=20" }
}
