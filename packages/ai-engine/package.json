{
  "name": "@conversation-platform/ai-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-independent AI orchestration engine with retry, fallback, streaming, and cost tracking",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/provider-framework": "workspace:*",
    "@conversation-platform/model-registry": "workspace:*",
    "@conversation-platform/logger": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
