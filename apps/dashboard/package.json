{
  "name": "@conversation-platform/dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Admin and tenant dashboard for the Conversation Platform",
  "scripts": {
    "dev": "echo 'Dashboard dev placeholder'",
    "build": "echo 'Dashboard build placeholder - install next to build'",
    "start": "next start",
    "test": "echo 'Dashboard tests placeholder'",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf .next out"
  },
  "engines": {
    "node": ">=20"
  }
}
