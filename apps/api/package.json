{
  "name": "@conversation-platform/api",
  "version": "0.1.0",
  "private": true,
  "description": "Core API server for the Conversation Platform",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm,cjs",
    "start": "node dist/index.js",
    "test": "vitest run",
    "lint": "eslint src/",
    "clean": "rimraf dist"
  },
  "engines": {
    "node": ">=20"
  }
}
