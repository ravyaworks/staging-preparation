name: conversation_platform_sdk
description: Flutter SDK for the Conversation Platform (placeholder).
version: 0.1.0
homepage: https://github.com/anomalyco/conversation-platform

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
