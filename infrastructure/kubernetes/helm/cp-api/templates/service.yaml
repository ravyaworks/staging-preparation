apiVersion: v1
kind: Service
metadata:
  name: {{ include "cp-api.fullname" . }}
  labels:
    {{- include "cp-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cp-api.selectorLabels" . | nindent 4 }}
