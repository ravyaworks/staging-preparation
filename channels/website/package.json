{
  "name": "@conversation-platform/channel-website",
  "version": "0.1.0",
  "private": true,
  "description": "Website widget channel - embeddable web chat with realtime messaging, typing indicators, and session management",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/channel-core": "workspace:*",
    "@conversation-platform/shared": "workspace:*",
    "@conversation-platform/logger": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
