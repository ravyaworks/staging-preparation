{
  "name": "@conversation-platform/channel-api",
  "version": "1.0.0",
  "private": true,
  "description": "Generic REST API channel for programmatic message send/receive",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/channel-core": "workspace:*",
    "@conversation-platform/shared": "workspace:*",
    "@conversation-platform/logger": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
