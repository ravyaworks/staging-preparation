{
  "name": "@conversation-platform/channel-discord",
  "version": "1.0.0",
  "private": true,
  "description": "Discord channel - bot messages, slash commands, embeds, attachments, and channels integration",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/channel-core": "workspace:*",
    "@conversation-platform/shared": "workspace:*",
    "@conversation-platform/logger": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
