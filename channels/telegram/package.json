{
  "name": "@conversation-platform/channel-telegram",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram channel - Bot API integration with support for commands, media, files, inline buttons, webhook and polling",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@conversation-platform/types": "workspace:*",
    "@conversation-platform/channel-core": "workspace:*",
    "@conversation-platform/shared": "workspace:*",
    "@conversation-platform/logger": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  }
}
